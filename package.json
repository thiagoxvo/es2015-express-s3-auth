{
  "name": "mediacomment",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "6.2.2"
  },
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/.bin/babel src/ -d build -s",
    "auto-build": "./node_modules/.bin/babel -w src/ -d build -s",
    "start": "node build/index.js",
    "auto-start": "./node_modules/.bin/nodemon --exec \"npm start\" --ignore build/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "nodemon": "^1.9.2"
  }
}
