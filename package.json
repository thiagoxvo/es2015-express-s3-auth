{
  "name": "mediacomment",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "6.2.2"
  },
  "main": "index.js",
  "scripts": {
    "build": "babel src/ -d build -s",
    "auto-build": "babel -w src/ -d build -s",
    "start": "node build/index.js",
    "auto-start": "nodemon --exec \"npm start\" --ignore build/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "nodemon": "^1.9.2"
  }
}
